include ../mixins/sub-menu.pug

header.header
  .container
    .row.align-items-center
      //- Logo
      .col-3
        .inner-logo
          a(href="/")
            img(src="/images/logo.jpeg", alt="Logo")

      //- Menu
      .col-4
        .inner-menu
          ul
            li
              a(href="/") Trang chủ
            li(class="sub-menu")
              - const pathProduct = "/products";
              a(href=pathProduct) Sản phẩm
              if (layoutCategoryProducts)
                +sub-menu(layoutCategoryProducts, pathProduct)
            li(class="sub-menu")
              - const pathPost = "/posts";
              a(href=pathPost) Bài viết
              if (layoutCategoryPosts)
                +sub-menu(layoutCategoryPosts, pathPost)

      //- Tìm kiếm
      .col-3
        form(action="/search" method="GET")
          div(class="form-group d-flex mb-0")
            input(
              type="text"
              placeholder="Nhập từ khóa..."
              class="form-control"
              name="keyword"
              value=keyword
            )
            button(
              type="submit"
              class="btn btn-primary ml-2"
            ) Tìm

      //- Giỏ hàng
      .col-2
        .inner-cart.text-right
          a(href="/cart" class="btn btn-outline-success")
            | Giỏ hàng (
            span #{miniCart ? miniCart.totalQuantity : 0}
            | )